dependencies {
    compile project(":career-api")
    compile "org.springframework.cloud:spring-cloud-starter-aws:$springCloudAWSVersion"

    testCompile "org.springframework.boot:spring-boot-starter-test:$springBootVersion"
}
